spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/flyway}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:password}
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
  flyway:
    enabled: true
    repair: true
    locations: classpath:db/migration/prod
